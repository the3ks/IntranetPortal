Prompt:
---
check my comments below and compose a new action plan:

Critical: Known default credentials are present across UI and backend provisioning => keep it unchanged.
High: Weak password policy at reset endpoint => keep it unchanged.
High: Sensitive config values are committed in app settings => keep it unchanged, since it's just for development env.
Medium: Cookie sharing strategy increases subdomain blast radius => keep it unchanged.
Answer to open questions:
1.cross-subdomain SSO a hard requirement => yes.
2. Do you want immediate JWT revocation on password reset/account disable => yes.
3. Is there a planned secret-management platform => no.

My recommendation:

password to be encrypted or hashed at browser side before being sent to server, the encrypted or hashed outcome must unique from time to time for the same password value.
password hashes in database also be different between users using the same password, recommend to have a salt value to be added and recorded for this purpose.

---

Updated Action Plan (Based On Your Decisions)

1. Keep unchanged by decision.
2. Critical default credentials flow.
3. Weak reset password policy.
4. Development appsettings secrets.
5. Cross-subdomain cookie sharing strategy (SSO is required).
6. Implement immediate JWT revocation on password reset and account disable.
7. Add a per-user credential version/security stamp column in user account table.
8. Include that version in JWT claims at login.
9. On every authorized request, compare token version with current DB version.
10. On password reset, increment version.
11. On account disable, increment version and set inactive.
12. Result: all previously issued tokens become invalid immediately.
13. Enforce active-account checks consistently.
14. Block login when account is inactive.
15. In authorization checks, deny inactive users even if token is otherwise valid.
16. Keep this check in both login and permission evaluation paths.
17. Strengthen identity integrity in DB.
18. Add normalized email handling (trim/lowercase).
19. Add unique index on normalized email.
20. Ensure all create/update/login paths use normalized email consistently.
21. Keep SSO-compatible cookie model, but harden surrounding controls.
22. Keep cross-subdomain cookie scope as-is.
23. Add strict origin allowlist review for all subdomains participating in SSO.
24. Add login rate limiting and lockout/backoff to reduce token abuse risk across sibling apps.
25. Address your password recommendations safely.
26. Browser-side “hash before send” should not replace server hashing and TLS.
27. If you still want replay-resistant client contribution, use a proper challenge-response protocol (not static client hash), but this is a larger project.
28. Your second recommendation is already covered if BCrypt is used correctly: BCrypt stores per-hash salt, so two users with same password naturally get different DB hashes.
29. Action: keep server-side BCrypt as source of truth; verify cost factor and rehash policy over time.
30. Practical rollout sequence.
31. Phase 1 (high impact, low disruption): JWT revocation versioning + inactive-user enforcement.
32. Phase 2: email normalization + unique index migration.
33. Phase 3: login rate limiting/lockout and security telemetry.
34. Phase 4 (optional): evaluate challenge-response design if you want client-side cryptographic hardening beyond TLS.


If you want, I can turn this into a concrete implementation checklist mapped file-by-file next.