# Login Security Hardening — Implementation Checklist

Date: 2026-04-16 | Implemented: 2026-04-16
Based on: `login_security_hardeness.md` + action plan agreed in session.

---

## Phase 1 — Immediate Token Revocation + Active User Enforcement

### 1.1 Add token version / security stamp field to user accounts

- [x] Add `int SecurityStamp` (default `1`) field to model  
  `backend/IntranetPortal.Data/Models/Core/UserAccount.cs`
- [x] Configure column mapping and default value  
  `backend/IntranetPortal.Data/Data/Configurations/Core/UserAccountConfiguration.cs`
- [x] Create EF migration  
  `backend/IntranetPortal.Data/Migrations/20260416125408_AddSecurityStampAndLockout.cs`

### 1.2 Include token version in JWT at login

- [x] Emit `SecurityStamp` as a claim inside `GenerateJwt()`  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`

### 1.3 Validate token version on every protected request

- [x] Compare `SecurityStamp` claim against current DB value in `HandleRequirementAsync()`  
  `backend/IntranetPortal.Api/Security/PermissionAuthorizationHandler.cs`
- [x] Deny authorization when claim value does not match DB value

### 1.4 Revoke all active sessions on password reset

- [x] Increment `SecurityStamp` when resetting a password  
  `backend/IntranetPortal.Api/Controllers/Core/UsersController.cs`

### 1.5 Revoke all active sessions on account disable

- [x] Increment `SecurityStamp` whenever `IsActive` is set to `false`  
  `backend/IntranetPortal.Api/Controllers/Core/EmployeesController.cs`

### 1.6 Block login for inactive accounts

- [x] Add `u.IsActive` check to login query before BCrypt verification  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`

### 1.7 Deny permission checks for inactive users

- [x] Add `u.IsActive == true` filter to both DB queries in auth handler  
  `backend/IntranetPortal.Api/Security/PermissionAuthorizationHandler.cs`

---

## Phase 2 — Email Identity Integrity

### 2.1 Normalize email at all write and lookup points

- [x] Normalize (trim + lowercase) email in login lookup  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`
- [x] Normalize on employee create/update  
  `backend/IntranetPortal.Api/Controllers/Core/EmployeesController.cs`
- [x] Normalize on CSV import and quick-setup flows  
  `backend/IntranetPortal.Api/Controllers/Core/SetupController.cs`

### 2.2 Enforce uniqueness in database

- [x] Add unique index on email column  
  `backend/IntranetPortal.Data/Data/Configurations/Core/UserAccountConfiguration.cs`
- [x] Create EF migration for uniqueness index  
  `backend/IntranetPortal.Data/Migrations/20260416125408_AddSecurityStampAndLockout.cs`
- [ ] ⚠️ Add duplicate-cleanup script (run before migration if existing data may have dupes)  
  `_release/database-update.sql`

---

## Phase 3 — Login Abuse Protection

### 3.1 Add login rate limiting

- [x] Register `AddRateLimiter` with a fixed-window or sliding-window policy (10 req/min)  
  `backend/IntranetPortal.Api/Program.cs`
- [x] Apply `[EnableRateLimiting("login")]` attribute to login endpoint  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`

### 3.2 Add account lockout / exponential backoff

- [x] Add `int FailedLoginAttempts` and `DateTimeOffset? LockedUntil` fields to model  
  `backend/IntranetPortal.Data/Models/Core/UserAccount.cs`
- [x] Add login logic:
  - Check `LockedUntil` before verifying password
  - Increment `FailedLoginAttempts` on failure
  - Lock account after 10 failures for 15 minutes
  - Reset counter on successful login  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`
- [x] Create EF migration for new lockout fields  
  `backend/IntranetPortal.Data/Migrations/20260416125408_AddSecurityStampAndLockout.cs`

---

## Phase 4 — Password Handling Clarification and Validation

### 4.1 Confirm BCrypt per-user salt is in place (already done)

- [x] Verify BCrypt hashing at login + reset remains unchanged — no action needed  
  `backend/IntranetPortal.Api/Controllers/Core/AuthController.cs`  
  `backend/IntranetPortal.Api/Controllers/Core/UsersController.cs`
- [x] Note: BCrypt generates a unique salt per hash automatically. Two users with the  
  same password will always produce different hash strings in the database. ✅

### 4.2 Planned — Challenge-response client-side crypto track

- [x] Open design task for a proper challenge-response protocol track.
- [x] Add API endpoints for challenge issue and challenge verify (module-scoped DTOs only).
- [x] Bind challenge to nonce + timestamp + one-time use + account context.
- [x] Add strict replay protection and short expiration window (60 seconds).
- [x] Keep TLS mandatory in all environments: challenge-response is defense-in-depth only.
- [ ] Add explicit threat model doc: challenge-response alone does not stop active MITM.
- [x] Add DB migration for challenge state table: `20260416134358_AddCoreLoginChallenges`

Implementation note:
- Current implementation uses one-time challenge + RSA-OAEP encrypted password transfer behind feature flag `ENABLE_CHALLENGE_LOGIN=true`.
- Planned next upgrade remains OPAQUE/SRPP-grade PAKE for stronger cryptographic guarantees.

---

## Verification Checklist (Run After Implementation)

- [ ] Inactive user **cannot** log in (returns 401)
- [ ] Valid active user with correct password **can** log in
- [ ] Login fails gracefully after 10 failed attempts (account locked 15 min, returns 401)
- [ ] Rate limiter returns HTTP 429 after >10 requests in a 1-minute window
- [ ] Password reset **immediately** invalidates previously issued tokens
- [ ] Account disable **immediately** invalidates previously issued tokens
- [ ] Two accounts with the same password store **different** BCrypt hashes
- [ ] Duplicate email insert/update **fails at DB layer** with constraint error
- [ ] Repeated login failures trigger **lockout** response (429 or 401 with backoff message)
- [ ] Existing SSO cross-subdomain cookie behavior **remains intact**
- [ ] Migration `20260416125408_AddSecurityStampAndLockout` applies cleanly (`dotnet ef database update`)
- [ ] ⚠️ Verify no duplicate emails in `Core_UserAccounts` before running migration on existing DB

If you want, I can now turn this into a task-by-task execution plan with exact code diffs per file in implementation order.
